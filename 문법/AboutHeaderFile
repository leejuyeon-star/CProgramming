/*
    헤더파일
    
    헤더파일은 소스가 컴파일되기 전 링크되는 코드이다.

    어디서 헤더파일을 가져오느냐에 따른 형식 구분
    - 표준 헤더파일이 저장된 디렉터리에서 가져오는 경우 
        : #include <헤더파일명>
    - 현재 디렉토리에서 가져오는 경우 (보통 프로그래머가 정의한 헤더파일) 
        : #include "해더페일명 or 경로"

*/

// 구조체가 중복 정의되는 경우를 막기 위해 
// 모든 헤더파일을 #ifndef~#endif로 감싸는 것이 안전하다
#ifndef HEADERFILE
#define HEADERFILE


// 구조체 정의는 소스파일이 아닌 헤더파일에 넣는다
typedef struct STRUCT
{
    int a;
} STRUC;


#endif
